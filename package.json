{
  "name": "technovision-erp",
  "version": "1.0.0",
  "description": "Technovision ERP System",
  "scripts": {
    "install": "cd backend && npm install && cd ../frontend && npm install --include=dev",
    "build": "cd frontend && npm run build",
    "start": "cd backend && npm start"
  }
}
